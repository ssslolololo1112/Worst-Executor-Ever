# Worst-Executor-Ever
u should use it not the best looking but it is decent,
UNC : 99%,
SUNC : idk i will tell u later when i find out,
LEVEL : 8,
API: QuarumAPI,
Beta.

To download the executor look at the left and press releases click the "latest" button then scroll down [ Make sure you read everything since most of the information for the executor is in the main "Release" thing ] and press Worst-Executor-Ever.zip, after it installs extract it to disk D: or C: your wish, open the folder press scroll down and find "Worst-Executor-Ever" press the "allow" or "yes" button in the Administrator Permissions wait for the executor then you are set.

UNC : 99% - 98%
sUNC : not sure
Level : 8
API : VelocityAPI
Injection method : erto3e4rortoergn
Open-source? : No
Virus? : No it is not a virus [ if you put the .zip file in virus.total, it will show alot of flags but those are false positives every executor has false positives no matter what i think ]

Q and A
Q: Can i use the API for my executor?
A: Yes you can use the API for your custom executor.

Q: Can i use the executor's code?
A: Sorry but no you can't use the code as it is not open-source

Q: Why does virus.total flag it as a malicious file?
A: Those red flags are false positives. Every executor that you download has false positives, no matter what executor you download it will always have 1 or 2 red flags [ False Positives ]

Q: Why does it ask for Administrator Permissions?
A: Because VelocityAPI cannot function properly without Administrator Permissions
